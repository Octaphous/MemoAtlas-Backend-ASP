meta {
  name: create prompt
  type: http
  seq: 4
}

post {
  url: {{host}}/api/prompts
  body: json
  auth: inherit
}

body:json {
  {
    "question": "Hur långt har du gått idag?",
    "type": 0,
    "private": false
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
