meta {
  name: create tag group
  type: http
  seq: 5
}

post {
  url: {{host}}/api/tag-groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "aktiviteter",
    "color": "blue",
    "private": false
  }
}

settings {
  encodeUrl: true
}
