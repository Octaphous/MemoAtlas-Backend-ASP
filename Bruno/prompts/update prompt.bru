meta {
  name: update prompt
  type: http
  seq: 4
}

patch {
  url: {{host}}/api/prompts/:id
  body: json
  auth: inherit
}

params:path {
  id: 1
}

body:json {
  {
    "question": "Hur många km har du gått idag?",
    "private": false
  }
}

settings {
  encodeUrl: true
}
