meta {
  name: create tag group
  type: http
  seq: 3
}

post {
  url: {{host}}/api/tag-groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "aktiviteter",
    "color": "blue"
  }
}

settings {
  encodeUrl: true
}
